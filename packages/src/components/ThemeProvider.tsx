'use client';

/**
 * AEGIS Theme Provider
 * Re-export from hooks for convenient importing
 */

export { ThemeProvider } from '../hooks/useTheme';
export type { ThemeProviderProps } from '../hooks/useTheme';
